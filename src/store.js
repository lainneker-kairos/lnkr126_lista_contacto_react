export const initialStore = () => {
  return {
    message: null,
    todos: [
      {
        id: 1,
        title: "Make the bed",
        background: null,
      },
      {
        id: 2,
        title: "Do my homework",
        background: null,
      }
    ],
    contacts: [] // Agregamos la lista de contactos al estado inicial
  }
}

export default function storeReducer(store, action = {}) {
  switch (action.type) {
    case 'add_task':
      const { id, color } = action.payload
      return {
        ...store,
        todos: store.todos.map((todo) => (todo.id === id ? { ...todo, background: color } : todo))
      };

    // Nueva acción para establecer los contactos en el estado global
    case 'set_contacts':
      return {
        ...store,
        contacts: action.payload
      };

    default:
      throw Error('Unknown action.');
  }
}

